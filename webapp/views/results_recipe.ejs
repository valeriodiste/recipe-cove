<!DOCTYPE html>
<html lang="en">
<head>
    <% if (info!=""){ %>
        <link rel="stylesheet" href="css/results_recipe.css" type="text/css">
        <link rel="stylesheet" href="css/style.css" type="text/css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Results</title>
</head>
<body>
    <div class="my-navbar">
        <h3 class="recipe-cove-logo" onclick="location.href='/'">
            Recipe Cove
        </h3>
        <div class="my-navbar-menù">
            <!-- <a href="/">Home</a> -->
            <%if (connected==true){%>
                <a href="profile">Profile</a>
                <form action='logout'>
                    <button>LOGOUT</button>
                </form> 
            <%}else{%>
                <div style="display:none">><a href="profile">Profile</a></div>
                <form action='login'>
                    <button>LOGIN</button>
                </form> 
            <%}%>
        </div>
    </div>

    <div class="main">
        <div class="main-box soft-shadow">
            <br>
            <h1>Search results for "<%= 
				<!-- Name is in the query parameter "search" of the URL -->
				search
				%>"</h1>
            <br>
            <div class="recipe-box">
                <% if(info.results.length>0){ %>
                    <% for(var i=0;i < info.results.length; i++) {%>
                        <div class="recipe-external-card">
                            <a href="https://localhost:3000/results_title?id=<%=info.results[i].id%>" style='text-decoration: none;'>
                                <button class="recipe-internal-card soft-shadow">
                                    <img class="immagine" src="<%= info.results[i].image%>">
                                    <br><br>
                                    <%= info.results[i].title%>
                                </button>
                            </a>
                        </div>
                    <%}%>
                <%}%>
            <%}%>
            </div>
            <br>
        </div>

    </div>
            
</body>
</html>    